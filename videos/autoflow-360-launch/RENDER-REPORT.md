# AutoFlow 360 演示视频成片报告

成片日期：2026-08-08。完整预览已获用户确认，随后使用本地 HyperFrames 0.7.87 与 FFmpeg 8.1.1 进行高质量渲染和成片验收。

## 成片信息

| 项目 | 结果 |
| --- | --- |
| 本地文件 | `renders/video.mp4`（由 `.gitignore` 排除，不写入 Git 历史） |
| 文件大小 | 65,534,062 字节（62.498 MB） |
| SHA-256 | `2EBF2BE882A0DCC5FA54675DE0D8229CBEEEB6D399B2DD322856048183851F58` |
| 容器时长 | 162.633333 秒 |
| 画面 | H.264 High、1920×1080、30fps、4879 帧、yuv420p、BT.709、逐行扫描 |
| 声音 | AAC-LC、48kHz、双声道、约 192kbps |
| 响度 | -17.1 LUFS；响度范围 2.3 LU；True Peak -2.1 dBFS |

时间线声明为 162.624 秒；MP4 容器多出的约 0.009 秒来自 30fps 帧边界取整，音视频流均为 162.633 秒，不存在异常尾音或尾帧。

## 验收结果

- HyperFrames 高质量渲染完成，4879/4879 帧全部捕获并成功封装。
- 使用 FFmpeg 对完整视频和音轨进行全程解码，未发现解码错误。
- 黑帧扫描阈值为连续 0.20 秒、像素暗度 10%，未检出黑帧。
- 静音扫描阈值为 -45dB、连续 1 秒，未检出异常长静音。
- 从最终 MP4 每约 8.2 秒抽帧形成 20 格总览，未见空白、错帧、字体缺失或字幕越界。
- 单独复核工程证据到 CTA 的最后 8.2 秒，品牌标识、结尾文案和最终停留帧完整。

## 发布边界

仓库保留完整可复现的视频源工程、配音、字幕时间、素材审计、离线字体与动画依赖。为避免 62.498 MB 二进制永久写入 Git 历史，正式公开发布时将 `video.mp4` 作为 GitHub Release 附件上传，并用本报告中的 SHA-256 核对上传结果。
